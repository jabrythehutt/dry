import { NoteExtractPipe } from './note-extract.pipe';

describe('NoteExtractPipe', () => {
  it('create an instance', () => {
    const pipe = new NoteExtractPipe();
    expect(pipe).toBeTruthy();
  });
});
